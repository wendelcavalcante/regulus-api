export declare function getPageNumbers({ currentPageState, prevPageNumbers, paginationRange, maxPage }: {
    currentPageState: number;
    prevPageNumbers?: number[];
    paginationRange: number;
    maxPage: number;
}): number[];
